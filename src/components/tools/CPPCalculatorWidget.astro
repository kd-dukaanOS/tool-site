---
import CalculatorField from "../calculator/CalculatorField.astro";
import CalculatorActions from "../calculator/ActionButtons.astro";
import EmptyState from "../calculator/EmptyState.astro";
import ResultCard from "../calculator/ResultCard.astro";
import ResultsGrid from "../calculator/ResultsGrid.astro";
import { PiggyBank, Users, Building2, Coins } from "@lucide/astro";
---
<section class="cpp-calculator">
  <div class="cpp-layout">
    <aside class="cpp-sidebar">
      <CalculatorField id="grossIncome" label="Gross Annual Income" type="number" required={true} helpText="Your gross annual income (CAD)." />
      <CalculatorActions calculateId="calculateBtn" resetId="resetBtn" copyId="copyBtn" showCopy={true} />
      <div id="errorBox" class="error-box" hidden></div>
    </aside>
    <main class="cpp-content">
      <div id="emptyState">
        <EmptyState icon="🏦" title="CPP Calculator" description="Estimate your Canada Pension Plan contribution." tips={["Pensionable Earnings", "Employee Portion", "Employer Portion"]} />
      </div>
      <div id="resultsContainer" hidden>
        <ResultsGrid columns={2}>
          <ResultCard id="pensionableResult" title="Pensionable Earnings" icon={PiggyBank} />
          <ResultCard id="employeeResult" title="Employee Contribution" icon={Users} subtitle=" " />
          <ResultCard id="employerResult" title="Employer Contribution" icon={Building2} />
          <ResultCard id="totalResult" title="Total Contribution" icon={Coins} />
        </ResultsGrid>
        <section class="calculator-info">
          <h2>About this Calculator</h2>
          <p>Estimates your CPP contribution after the basic exemption, capped at the yearly maximum pensionable earnings. Excludes CPP2. Verify against current CRA figures.</p>
        </section>
      </div>
    </main>
  </div>
</section>
<script>
  import "../../scripts/ca-cpp-calculator";
</script>
<style>
.cpp-calculator{width:100%;}
.cpp-layout{display:grid;grid-template-columns:340px minmax(0,1fr);gap:32px;align-items:start;}
.cpp-sidebar{position:sticky;top:24px;display:flex;flex-direction:column;gap:20px;}
.cpp-content{display:flex;flex-direction:column;gap:28px;min-width:0;}
.calculator-info{padding:24px;border:1px solid var(--border-subtle);border-radius:18px;background:var(--bg-card);}
.calculator-info h2{margin:0 0 12px;font-size:1.15rem;font-weight:700;color:var(--text-primary);}
.calculator-info p{margin:0;line-height:1.8;font-size:.95rem;color:var(--text-secondary);}
.error-box{display:flex;align-items:center;padding:14px 16px;border-radius:14px;background:#FEF2F2;border:1px solid #FCA5A5;color:#B91C1C;font-size:.92rem;font-weight:600;}
:global(.dark) .error-box{background:#2A1111;border-color:#7F1D1D;color:#FECACA;}
@media(max-width:1024px){.cpp-layout{grid-template-columns:1fr;}.cpp-sidebar{position:static;}}
@media(max-width:768px){.cpp-layout{gap:24px;}.cpp-content{gap:20px;}}
@media(max-width:640px){.calculator-info{padding:18px;}}
</style>