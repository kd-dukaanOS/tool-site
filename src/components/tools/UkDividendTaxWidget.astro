---
import CalculatorField from "../calculator/CalculatorField.astro";
import CalculatorActions from "../calculator/ActionButtons.astro";
import EmptyState from "../calculator/EmptyState.astro";
import ResultCard from "../calculator/ResultCard.astro";
import ResultsGrid from "../calculator/ResultsGrid.astro";
import { PoundSterling, Percent, Wallet, ShieldCheck } from "@lucide/astro";
---
<section class="ukdt-calculator">
  <div class="ukdt-layout">
    <aside class="ukdt-sidebar">
      <div class="ukdt-group">
        <h3>Tax Year</h3>
        <CalculatorField type="select" id="taxYear" label="">
          <option value="2026-27">2026/27 (current)</option>
          <option value="2025-26">2025/26</option>
          <option value="2024-25">2024/25</option>
        </CalculatorField>
      </div>
      <div class="ukdt-group">
        <h3>Income</h3>
        <CalculatorField id="otherIncome" label="Other Taxable Income (salary, pension, etc.)" type="number" placeholder="e.g. 12570" min="0" step="1" />
        <CalculatorField id="dividendIncome" label="Dividend Income" type="number" placeholder="e.g. 20000" min="0" step="1" />
      </div>
      <CalculatorActions calculateId="calculateBtn" resetId="resetBtn" copyId="copyBtn" showCopy={true} />
      <div id="errorBox" class="error-box" hidden></div>
    </aside>
    <main class="ukdt-content">
      <div id="emptyState">
        <EmptyState icon="💷" title="UK Dividend Tax Calculator" description="Work out tax on your UK dividend income for the current and recent tax years." tips={["Total Tax", "Net Dividend", "Effective Rate"]} />
      </div>
      <div id="resultsContainer" hidden>
        <ResultsGrid columns={4}>
          <ResultCard id="totalTaxResult" title="Total Dividend Tax" icon={PoundSterling} accent="warning" subtitle=" " />
          <ResultCard id="netDividendResult" title="Net Dividend Income" icon={Wallet} accent="success" />
          <ResultCard id="effectiveRateResult" title="Effective Rate" icon={Percent} />
          <ResultCard id="allowanceUsedResult" title="Tax-Free Allowance Used" icon={ShieldCheck} />
        </ResultsGrid>
        <section class="calculator-info">
          <h2>About this Calculator</h2>
          <p>Stacks your dividend income on top of your other taxable income, applies your Personal Allowance (with tapering above £100,000), the £500 dividend allowance, and the correct basic/higher/additional rate bands for the selected UK tax year.</p>
        </section>
      </div>
    </main>
  </div>
</section>
<script>
  import "../../scripts/uk-dividend-tax-calculator";
</script>
<style>
.ukdt-calculator{width:100%;}
.ukdt-layout{display:grid;grid-template-columns:340px minmax(0,1fr);gap:32px;align-items:start;}
.ukdt-sidebar{position:sticky;top:24px;display:flex;flex-direction:column;gap:20px;max-height:calc(100vh - 48px);overflow-y:auto;}
.ukdt-content{display:flex;flex-direction:column;gap:28px;min-width:0;}
.ukdt-group{display:flex;flex-direction:column;gap:12px;padding:16px;border:1px solid var(--border-subtle);border-radius:14px;background:var(--bg-card);}
.ukdt-group h3{margin:0;font-size:.9rem;font-weight:700;color:var(--text-primary);}
.calculator-info{padding:24px;border:1px solid var(--border-subtle);border-radius:18px;background:var(--bg-card);}
.calculator-info h2{margin:0 0 12px;font-size:1.15rem;font-weight:700;color:var(--text-primary);}
.calculator-info p{margin:0;line-height:1.8;font-size:.95rem;color:var(--text-secondary);}
.error-box{display:flex;align-items:center;padding:14px 16px;border-radius:14px;background:#FEF2F2;border:1px solid #FCA5A5;color:#B91C1C;font-size:.92rem;font-weight:600;}
:global(.dark) .error-box{background:#2A1111;border-color:#7F1D1D;color:#FECACA;}
@media(max-width:1024px){.ukdt-layout{grid-template-columns:1fr;}.ukdt-sidebar{position:static;max-height:none;}}
@media(max-width:768px){.ukdt-layout{gap:24px;}.ukdt-content{gap:20px;}}
@media(max-width:640px){.calculator-info{padding:18px;}}
</style>