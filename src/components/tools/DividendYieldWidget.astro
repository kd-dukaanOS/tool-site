---
import CalculatorField from "../calculator/CalculatorField.astro";
import CalculatorActions from "../calculator/ActionButtons.astro";
import EmptyState from "../calculator/EmptyState.astro";
import ResultCard from "../calculator/ResultCard.astro";
import ResultsGrid from "../calculator/ResultsGrid.astro";
import { Percent, DollarSign, Wallet, TrendingUp } from "@lucide/astro";
---
<section class="dy-calculator">
  <div class="dy-layout">
    <aside class="dy-sidebar">
      <div class="dy-group">
        <h3>Stock Info</h3>
        <CalculatorField id="sharePrice" label="Current Share Price" type="number" placeholder="e.g. 50" min="0" step="0.01" />
        <CalculatorField id="annualDividendPerShare" label="Annual Dividend per Share" type="number" placeholder="e.g. 2.00" min="0" step="0.01" />
        <CalculatorField id="numberOfShares" label="Number of Shares" type="number" placeholder="e.g. 100" min="0" step="1" />
      </div>
      <div class="dy-group">
        <h3>Yield on Cost (Optional)</h3>
        <CalculatorField id="purchasePrice" label="Your Purchase Price" type="number" placeholder="e.g. 40" min="0" step="0.01" />
      </div>
      <CalculatorActions calculateId="calculateBtn" resetId="resetBtn" copyId="copyBtn" showCopy={true} />
      <div id="errorBox" class="error-box" hidden></div>
    </aside>
    <main class="dy-content">
      <div id="emptyState">
        <EmptyState icon="💵" title="Dividend Yield Calculator" description="Find your dividend yield and projected income." tips={["Dividend Yield", "Annual Income", "Yield on Cost"]} />
      </div>
      <div id="resultsContainer" hidden>
        <ResultsGrid columns={4}>
          <ResultCard id="dividendYieldResult" title="Dividend Yield" icon={Percent} accent="success" />
          <ResultCard id="annualIncomeResult" title="Annual Dividend Income" icon={DollarSign} />
          <ResultCard id="monthlyIncomeResult" title="Monthly Dividend Income" icon={Wallet} />
          <ResultCard id="yieldOnCostResult" title="Yield on Cost" icon={TrendingUp} subtitle=" " />
        </ResultsGrid>
        <section class="calculator-info">
          <h2>About this Calculator</h2>
          <p>Calculates a stock's dividend yield, your projected annual and monthly dividend income, and yield on cost based on your original purchase price.</p>
        </section>
      </div>
    </main>
  </div>
</section>
<script>
  import "../../scripts/dividend-yield-calculator";
</script>
<style>
.dy-calculator{width:100%;}
.dy-layout{display:grid;grid-template-columns:340px minmax(0,1fr);gap:32px;align-items:start;}
.dy-sidebar{position:sticky;top:24px;display:flex;flex-direction:column;gap:20px;max-height:calc(100vh - 48px);overflow-y:auto;}
.dy-content{display:flex;flex-direction:column;gap:28px;min-width:0;}
.dy-group{display:flex;flex-direction:column;gap:12px;padding:16px;border:1px solid var(--border-subtle);border-radius:14px;background:var(--bg-card);}
.dy-group h3{margin:0;font-size:.9rem;font-weight:700;color:var(--text-primary);}
.calculator-info{padding:24px;border:1px solid var(--border-subtle);border-radius:18px;background:var(--bg-card);}
.calculator-info h2{margin:0 0 12px;font-size:1.15rem;font-weight:700;color:var(--text-primary);}
.calculator-info p{margin:0;line-height:1.8;font-size:.95rem;color:var(--text-secondary);}
.error-box{display:flex;align-items:center;padding:14px 16px;border-radius:14px;background:#FEF2F2;border:1px solid #FCA5A5;color:#B91C1C;font-size:.92rem;font-weight:600;}
:global(.dark) .error-box{background:#2A1111;border-color:#7F1D1D;color:#FECACA;}
@media(max-width:1024px){.dy-layout{grid-template-columns:1fr;}.dy-sidebar{position:static;max-height:none;}}
@media(max-width:768px){.dy-layout{gap:24px;}.dy-content{gap:20px;}}
@media(max-width:640px){.calculator-info{padding:18px;}}
</style>