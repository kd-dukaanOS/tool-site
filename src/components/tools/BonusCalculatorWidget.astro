{/* src/components/tools/BonusCalculatorWidget.astro */}
---
import CalculatorField from "../calculator/CalculatorField.astro";
import CalculatorActions from "../calculator/ActionButtons.astro";
import EmptyState from "../calculator/EmptyState.astro";
import ResultCard from "../calculator/ResultCard.astro";
import ResultsGrid from "../calculator/ResultsGrid.astro";

import { Gift, Wallet, Receipt, TrendingUp } from "@lucide/astro";
---

<section class="bonus-calculator">
  <div class="bonus-layout">

    <aside class="bonus-sidebar">
      <CalculatorField
        id="annualSalary"
        label="Annual Salary ($)"
        type="number"
        placeholder="e.g. 60000"
        required={true}
        min="0"
        step="1"
        helpText="Your base annual salary."
      />

      <CalculatorField
        id="bonusPercent"
        label="Bonus (%)"
        type="number"
        placeholder="e.g. 10"
        required={true}
        min="0"
        step="0.1"
        helpText="Bonus as a percentage of salary."
      />

      <CalculatorField
        id="taxRate"
        label="Tax Rate on Bonus (%)"
        type="number"
        placeholder="e.g. 22"
        min="0"
        max="100"
        step="0.1"
        helpText="Estimated tax rate applied to bonus pay."
      />

      <CalculatorActions
        calculateId="calculateBtn"
        resetId="resetBtn"
        copyId="copyBtn"
        showCopy={true}
      />

      <div id="errorBox" class="error-box" hidden></div>
    </aside>

    <main class="bonus-content">
      <div id="emptyState">
        <EmptyState
          icon="🎁"
          title="Bonus Calculator"
          description="Find your gross bonus, tax on bonus, and net take-home bonus amount."
          tips={["Net Bonus", "Gross Bonus", "Tax on Bonus", "Total Compensation"]}
        />
      </div>

      <div id="resultsContainer" hidden>
        <ResultsGrid columns={4}>
          <ResultCard id="netBonusResult" title="Net Bonus" icon={Wallet} accent="success" size="lg" />
          <ResultCard id="bonusAmountResult" title="Gross Bonus" icon={Gift} accent="primary" />
          <ResultCard id="taxOnBonusResult" title="Tax on Bonus" icon={Receipt} />
          <ResultCard id="totalCompResult" title="Total Compensation" icon={TrendingUp} />
        </ResultsGrid>

        <section class="calculator-info">
          <h2>About this Calculator</h2>
          <p>
            Calculates your gross bonus, estimated tax on the bonus, and net take-home bonus amount
            from your salary and bonus percentage. All calculations run locally — no external data
            or paid service is required.
          </p>
        </section>
      </div>
    </main>

  </div>
</section>

<script>
  import "../../scripts/bonus-calculator";
</script>

<style>
.bonus-calculator{ width:100%; }
.bonus-layout{ display:grid; grid-template-columns:340px minmax(0,1fr); gap:32px; align-items:start; }
.bonus-sidebar{ position:sticky; top:24px; display:flex; flex-direction:column; gap:20px; }
.bonus-content{ display:flex; flex-direction:column; gap:28px; min-width:0; }
.calculator-info{ padding:24px; border:1px solid var(--border-subtle); border-radius:18px; background:var(--bg-card); }
.calculator-info h2{ margin:0 0 12px; font-size:1.15rem; font-weight:700; color:var(--text-primary); }
.calculator-info p{ margin:0; line-height:1.8; font-size:.95rem; color:var(--text-secondary); }
.error-box{ display:flex; align-items:center; padding:14px 16px; border-radius:14px; background:#FEF2F2; border:1px solid #FCA5A5; color:#B91C1C; font-size:.92rem; font-weight:600; }
:global(.dark) .error-box{ background:#2A1111; border-color:#7F1D1D; color:#FECACA; }
@media(max-width:1024px){ .bonus-layout{ grid-template-columns:1fr; } .bonus-sidebar{ position:static; } }
@media(max-width:768px){ .bonus-layout{ gap:24px; } .bonus-content{ gap:20px; } }
@media(max-width:640px){ .calculator-info{ padding:18px; } }
</style>