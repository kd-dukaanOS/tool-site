---
import CalculatorField from "../calculator/CalculatorField.astro";
import CalculatorActions from "../calculator/ActionButtons.astro";
import EmptyState from "../calculator/EmptyState.astro";
import ResultCard from "../calculator/ResultCard.astro";
import ResultsGrid from "../calculator/ResultsGrid.astro";
import { Landmark, TrendingUp, FileText, Wallet } from "@lucide/astro";
---
<section class="ukit-calculator">
  <div class="ukit-layout">
    <aside class="ukit-sidebar">
      <CalculatorField id="grossIncome" label="Gross Annual Salary" type="number" required={true} helpText="Your gross salary before deductions (GBP)." />
      <div class="ukit-region-note">
        <p>Uses England, Wales &amp; Northern Ireland tax bands. Scotland has separate bands not yet supported.</p>
      </div>
      <CalculatorActions calculateId="calculateBtn" resetId="resetBtn" copyId="copyBtn" showCopy={true} />
      <div id="errorBox" class="error-box" hidden></div>
    </aside>
    <main class="ukit-content">
      <div id="emptyState">
        <EmptyState icon="🇬🇧" title="UK Income Tax Calculator" description="See your estimated Income Tax, effective rate, and marginal rate." tips={["Income Tax Owed", "Effective Rate", "Marginal Rate"]} />
      </div>
      <div id="resultsContainer" hidden>
        <ResultsGrid columns={2}>
          <ResultCard id="incomeTaxResult" title="Income Tax Owed" icon={Landmark} subtitle=" " />
          <ResultCard id="marginalRateResult" title="Marginal Tax Rate" icon={TrendingUp} />
          <ResultCard id="taxableIncomeResult" title="Taxable Income" icon={FileText} />
          <ResultCard id="afterTaxResult" title="After-Tax Income" icon={Wallet} />
        </ResultsGrid>
        <section class="calculator-info">
          <h2>About this Calculator</h2>
          <p>Estimates UK Income Tax using England/Wales/NI bands and the standard Personal Allowance. Excludes National Insurance — use the National Insurance Calculator for that. Personal Allowance taper above £100,000 is not modeled. Verify against current HMRC figures.</p>
        </section>
      </div>
    </main>
  </div>
</section>
<script>
  import "../../scripts/uk-income-tax";
</script>
<style>
.ukit-calculator{width:100%;}
.ukit-layout{display:grid;grid-template-columns:340px minmax(0,1fr);gap:32px;align-items:start;}
.ukit-sidebar{position:sticky;top:24px;display:flex;flex-direction:column;gap:20px;}
.ukit-content{display:flex;flex-direction:column;gap:28px;min-width:0;}
.ukit-region-note{padding:12px 14px;border-radius:10px;background:var(--bg-card);border:1px solid var(--border-subtle);}
.ukit-region-note p{margin:0;font-size:.78rem;color:var(--text-secondary);line-height:1.5;}
.calculator-info{padding:24px;border:1px solid var(--border-subtle);border-radius:18px;background:var(--bg-card);}
.calculator-info h2{margin:0 0 12px;font-size:1.15rem;font-weight:700;color:var(--text-primary);}
.calculator-info p{margin:0;line-height:1.8;font-size:.95rem;color:var(--text-secondary);}
:global(.dark) .error-box{background:#2A1111;border-color:#7F1D1D;color:#FECACA;}
@media(max-width:1024px){.ukit-layout{grid-template-columns:1fr;}.ukit-sidebar{position:static;}}
@media(max-width:768px){.ukit-layout{gap:24px;}.ukit-content{gap:20px;}}
@media(max-width:640px){.calculator-info{padding:18px;}}
</style>