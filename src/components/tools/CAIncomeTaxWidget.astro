---
import CalculatorField from "../calculator/CalculatorField.astro";
import CalculatorActions from "../calculator/ActionButtons.astro";
import EmptyState from "../calculator/EmptyState.astro";
import ResultCard from "../calculator/ResultCard.astro";
import ResultsGrid from "../calculator/ResultsGrid.astro";
import { Landmark, MapPin, TrendingUp, Wallet } from "@lucide/astro";
---
<section class="cait-calculator">
  <div class="cait-layout">
    <aside class="cait-sidebar">
      <CalculatorField id="grossIncome" label="Gross Annual Income" type="number" required={true} helpText="Your gross salary before deductions (CAD)." />
      <div class="cait-province">
        <label for="province">Province</label>
        <select id="province">
          <option value="ON">Ontario</option>
          <option value="AB">Alberta</option>
          <option value="BC">British Columbia</option>
        </select>
      </div>
      <CalculatorActions calculateId="calculateBtn" resetId="resetBtn" copyId="copyBtn" showCopy={true} />
      <div id="errorBox" class="error-box" hidden></div>
    </aside>
    <main class="cait-content">
      <div id="emptyState">
        <EmptyState icon="🇨🇦" title="Canada Income Tax Calculator" description="See your estimated federal + provincial income tax and combined marginal rate." tips={["Federal Tax", "Provincial Tax", "Marginal Rate"]} />
      </div>
      <div id="resultsContainer" hidden>
        <ResultsGrid columns={2}>
          <ResultCard id="federalTaxResult" title="Federal Tax" icon={Landmark} subtitle=" " />
          <ResultCard id="provincialTaxResult" title="Provincial Tax" icon={MapPin} />
          <ResultCard id="marginalRateResult" title="Combined Marginal Rate" icon={TrendingUp} />
          <ResultCard id="afterTaxResult" title="After-Tax Income" icon={Wallet} />
        </ResultsGrid>
        <section class="calculator-info">
          <h2>About this Calculator</h2>
          <p>Estimates combined federal and provincial income tax for Ontario, Alberta, and British Columbia. Does not include CPP or EI — use the Canada Take-Home Pay Calculator for a full paycheque breakdown. Verify against current CRA figures.</p>
        </section>
      </div>
    </main>
  </div>
</section>
<script>
  import "../../scripts/ca-income-tax";
</script>
<style>
.cait-calculator{width:100%;}
.cait-layout{display:grid;grid-template-columns:340px minmax(0,1fr);gap:32px;align-items:start;}
.cait-sidebar{position:sticky;top:24px;display:flex;flex-direction:column;gap:20px;}
.cait-content{display:flex;flex-direction:column;gap:28px;min-width:0;}
.cait-province{display:flex;flex-direction:column;gap:6px;}
.cait-province label{font-size:.85rem;font-weight:600;color:var(--text-primary);}
.cait-province select{padding:10px 12px;border-radius:10px;border:1px solid var(--border-subtle);background:var(--bg-card);color:var(--text-primary);font-size:.9rem;}
.calculator-info{padding:24px;border:1px solid var(--border-subtle);border-radius:18px;background:var(--bg-card);}
.calculator-info h2{margin:0 0 12px;font-size:1.15rem;font-weight:700;color:var(--text-primary);}
.calculator-info p{margin:0;line-height:1.8;font-size:.95rem;color:var(--text-secondary);}
:global(.dark) .error-box{background:#2A1111;border-color:#7F1D1D;color:#FECACA;}
@media(max-width:1024px){.cait-layout{grid-template-columns:1fr;}.cait-sidebar{position:static;}}
@media(max-width:768px){.cait-layout{gap:24px;}.cait-content{gap:20px;}}
@media(max-width:640px){.calculator-info{padding:18px;}}
</style>