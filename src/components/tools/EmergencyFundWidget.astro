---
import CalculatorField from "../calculator/CalculatorField.astro";
import CalculatorActions from "../calculator/ActionButtons.astro";
import EmptyState from "../calculator/EmptyState.astro";
import ResultCard from "../calculator/ResultCard.astro";
import ResultsGrid from "../calculator/ResultsGrid.astro";
import { Shield, PiggyBank, Wallet, Clock } from "@lucide/astro";
---
<section class="ef-calculator">
  <div class="ef-layout">
    <aside class="ef-sidebar">
      <div class="ef-group">
        <h3>Your Expenses</h3>
        <CalculatorField id="monthlyExpenses" label="Monthly Essential Expenses" type="number" placeholder="e.g. 3000" min="0" step="1" />
        <CalculatorField id="currentSavings" label="Current Emergency Savings" type="number" placeholder="e.g. 2000" min="0" step="1" />
      </div>
      <div class="ef-group">
        <h3>Goal</h3>
        <CalculatorField id="targetMonths" label="Target Months of Coverage" type="number" placeholder="e.g. 6" min="1" step="1" />
        <CalculatorField id="monthlySavingsCapacity" label="Monthly Savings Capacity" type="number" placeholder="e.g. 300" min="0" step="1" />
      </div>
      <CalculatorActions calculateId="calculateBtn" resetId="resetBtn" copyId="copyBtn" showCopy={true} />
      <div id="errorBox" class="error-box" hidden></div>
    </aside>
    <main class="ef-content">
      <div id="emptyState">
        <EmptyState icon="🛟" title="Emergency Fund Calculator" description="Enter your expenses and savings to see your target, progress, and time to reach your goal." tips={["Target Amount", "Progress %", "Time to Goal"]} />
      </div>
      <div id="resultsContainer" hidden>
        <ResultsGrid columns={4}>
          <ResultCard id="targetAmountResult" title="Target Amount" icon={Shield} accent="success" subtitle=" " />
          <ResultCard id="progressResult" title="Current Progress" icon={PiggyBank} />
          <ResultCard id="remainingResult" title="Remaining to Save" icon={Wallet} />
          <ResultCard id="monthsToGoalResult" title="Time to Goal" icon={Clock} />
        </ResultsGrid>
        <section class="calculator-info">
          <h2>About this Calculator</h2>
          <p>Calculates your emergency fund target based on monthly essential expenses and a chosen coverage period (commonly 3-6 months), then shows your current progress and how long it will take to reach the goal.</p>
        </section>
      </div>
    </main>
  </div>
</section>
<script>
  import "../../scripts/emergency-fund-calculator";
</script>
<style>
.ef-calculator{width:100%;}
.ef-layout{display:grid;grid-template-columns:340px minmax(0,1fr);gap:32px;align-items:start;}
.ef-sidebar{position:sticky;top:24px;display:flex;flex-direction:column;gap:20px;}
.ef-content{display:flex;flex-direction:column;gap:28px;min-width:0;}
.ef-group{display:flex;flex-direction:column;gap:12px;padding:16px;border:1px solid var(--border-subtle);border-radius:14px;background:var(--bg-card);}
.ef-group h3{margin:0;font-size:.9rem;font-weight:700;color:var(--text-primary);}
.calculator-info{padding:24px;border:1px solid var(--border-subtle);border-radius:18px;background:var(--bg-card);}
.calculator-info h2{margin:0 0 12px;font-size:1.15rem;font-weight:700;color:var(--text-primary);}
.calculator-info p{margin:0;line-height:1.8;font-size:.95rem;color:var(--text-secondary);}
.error-box{display:flex;align-items:center;padding:14px 16px;border-radius:14px;background:#FEF2F2;border:1px solid #FCA5A5;color:#B91C1C;font-size:.92rem;font-weight:600;}
:global(.dark) .error-box{background:#2A1111;border-color:#7F1D1D;color:#FECACA;}
@media(max-width:1024px){.ef-layout{grid-template-columns:1fr;}.ef-sidebar{position:static;}}
@media(max-width:768px){.ef-layout{gap:24px;}.ef-content{gap:20px;}}
@media(max-width:640px){.calculator-info{padding:18px;}}
</style>