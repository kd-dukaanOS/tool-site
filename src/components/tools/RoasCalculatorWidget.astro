
---
{/* src/components/tools/RoasCalculatorWidget.astro */}
import CalculatorField from "../calculator/CalculatorField.astro";
import CalculatorActions from "../calculator/ActionButtons.astro";
import EmptyState from "../calculator/EmptyState.astro";
import ResultCard from "../calculator/ResultCard.astro";
import ResultsGrid from "../calculator/ResultsGrid.astro";
import CurrencySelector from "../calculator/CurrencySelector.astro";

import { TrendingUp, Percent, DollarSign } from "@lucide/astro";
---

<section class="roas-calculator">
  <div class="roas-layout">

    <aside class="roas-sidebar">
      <CurrencySelector />

      <CalculatorField
        id="revenue"
        label="Revenue from Ads ($)"
        type="number"
        placeholder="e.g. 5000"
        required={true}
        min="0"
        step="0.01"
        helpText="Total revenue generated by the ad campaign."
      />

      <CalculatorField
        id="adSpend"
        label="Ad Spend ($)"
        type="number"
        placeholder="e.g. 1000"
        required={true}
        min="0"
        step="0.01"
        helpText="Total amount spent on the ad campaign."
      />

      <CalculatorActions
        calculateId="calculateBtn"
        resetId="resetBtn"
        copyId="copyBtn"
        showCopy={true}
      />

      <div id="errorBox" class="error-box" hidden></div>
    </aside>

    <main class="roas-content">
      <div id="emptyState">
        <EmptyState
          icon="📢"
          title="ROAS Calculator"
          description="Find your Return on Ad Spend (ROAS) and campaign profit from revenue and ad cost."
          tips={["ROAS", "Return %", "Profit"]}
        />
      </div>

      <div id="resultsContainer" hidden>
        <ResultsGrid columns={3}>
          <ResultCard id="roasResult" title="ROAS" icon={TrendingUp} accent="success" size="lg" />
          <ResultCard id="roasPercentResult" title="Return %" icon={Percent} accent="primary" />
          <ResultCard id="profitResult" title="Profit" icon={DollarSign} />
        </ResultsGrid>

        <section class="calculator-info">
          <h2>About this Calculator</h2>
          <p>
            Calculates Return on Ad Spend (ROAS) and campaign profit from ad revenue and ad spend.
            All calculations run locally — no external data or paid service is required.
          </p>
        </section>
      </div>
    </main>

  </div>
</section>

<script>
  import "../../scripts/roas-calculator";
</script>

<style>
.roas-calculator{ width:100%; }
.roas-layout{ display:grid; grid-template-columns:340px minmax(0,1fr); gap:32px; align-items:start; }
.roas-sidebar{ position:sticky; top:24px; display:flex; flex-direction:column; gap:20px; }
.roas-content{ display:flex; flex-direction:column; gap:28px; min-width:0; }
.calculator-info{ padding:24px; border:1px solid var(--border-subtle); border-radius:18px; background:var(--bg-card); }
.calculator-info h2{ margin:0 0 12px; font-size:1.15rem; font-weight:700; color:var(--text-primary); }
.calculator-info p{ margin:0; line-height:1.8; font-size:.95rem; color:var(--text-secondary); }
.error-box{ display:flex; align-items:center; padding:14px 16px; border-radius:14px; background:#FEF2F2; border:1px solid #FCA5A5; color:#B91C1C; font-size:.92rem; font-weight:600; }
:global(.dark) .error-box{ background:#2A1111; border-color:#7F1D1D; color:#FECACA; }
@media(max-width:1024px){ .roas-layout{ grid-template-columns:1fr; } .roas-sidebar{ position:static; } }
@media(max-width:768px){ .roas-layout{ gap:24px; } .roas-content{ gap:20px; } }
@media(max-width:640px){ .calculator-info{ padding:18px; } }
</style>