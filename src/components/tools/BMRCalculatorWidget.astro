---
import CalculatorField from "../calculator/CalculatorField.astro";
import ActionButtons from "../calculator/ActionButtons.astro";
import ResultCard from "../calculator/ResultCard.astro";
import ResultsGrid from "../calculator/ResultsGrid.astro";
import EmptyState from "../calculator/EmptyState.astro";
import { Flame } from "@lucide/astro";

interface Props {
  lang?: "en" | "es";
}
const { lang = "en" } = Astro.props as Props;

const copy = {
  en: {
    gender: "Gender", male: "Male", female: "Female",
    age: "Age", height: "Height (cm)", weight: "Weight (kg)",
    emptyTitle: "Enter details to calculate BMR", emptyDesc: "Your basal metabolic rate will appear here.",
    bmr: "BMR", atRest: "at complete rest",
  },
  es: {
    gender: "Sexo", male: "Hombre", female: "Mujer",
    age: "Edad", height: "Estatura (cm)", weight: "Peso (kg)",
    emptyTitle: "Ingresa tus datos para calcular tu TMB", emptyDesc: "Aquí verás tu tasa metabólica basal.",
    bmr: "TMB", atRest: "en reposo total",
  },
}[lang];
---

<div class="calculator-widget">
  <div class="calculator-inputs">
    <CalculatorField id="gender" label={copy.gender} type="select">
      <option value="male">{copy.male}</option>
      <option value="female">{copy.female}</option>
    </CalculatorField>

    <CalculatorField id="age" label={copy.age} type="number" placeholder="e.g. 28" />
    <CalculatorField id="heightCm" label={copy.height} type="number" placeholder="e.g. 170" />
    <CalculatorField id="weightKg" label={copy.weight} type="number" placeholder="e.g. 65" />

    <div id="errorBox" class="error-box" role="alert" hidden></div>

    <ActionButtons
      calculateId="calculateBtn"
      resetId="resetBtn"
      copyId="copyBtn"
      showDownload={false}
      showShare={false}
    />
  </div>

  <div class="calculator-output">
    <EmptyState
      id="emptyState"
      title={copy.emptyTitle}
      description={copy.emptyDesc}
    />

    <ResultsGrid id="resultsContainer" hidden>
      <ResultCard id="bmrResult" title={copy.bmr} icon={Flame} accent="primary" size="lg" />
    </ResultsGrid>
  </div>
</div>

<script define:vars={{ calcLang: lang }}>
  window.calcLang = calcLang;
</script>
<script src="../../scripts/bmr-calculator.ts"></script>
